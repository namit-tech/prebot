// Questions Data - Only Admin-added questions will be used
// No hardcoded questions - user must add questions via Admin panel
const questionsData = [];

// Export for use in other files
if (typeof module !== 'undefined' && module.exports) {
    module.exports = questionsData;
}
